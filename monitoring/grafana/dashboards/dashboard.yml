# Grafana dashboard provisioning
apiVersion: 1

providers:
  - name: 'CipherStudio Dashboards'
    orgId: 1
    folder: 'CipherStudio'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json